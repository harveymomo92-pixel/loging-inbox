# NEXT

## Immediate
1. Add time-range filters (today, 24h, 7d, custom range) to viewer and `/logs` API.
2. Add proper pagination/navigation (page, offset, next/prev) for large log volumes.
3. Add per-message detail view with raw payload JSON, media metadata, and processing status.

## After that
4. Improve support for non-image media (video/audio/document metadata and presentation).
5. Log and surface invalid webhook payloads for debugging and auditability.
6. Add export/query helpers (JSON/CSV) for automation and reporting.

## Later
7. Add storage hygiene tools (retention policy, orphan media detection, size monitoring).
8. Replace placeholder image context with real OCR/vision analysis when the pipeline is stable.
